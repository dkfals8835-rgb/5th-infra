# 기본 동작

# 실행 중인 컨테이너 확인
docker ps

# nginx 컨테이너 실행 (이미지 자동 pull)
docker run -d -p 80:80 nginx

# 실행 중인 컨테이너 확인
docker ps

# 컨테이너 중지
docker stop <컨테이너ID 또는 이름>

# 전체 컨테이너 확인 (중지 포함)
docker ps -a

# Docker 시스템 전체 정보
docker system info

# 컨테이너 로그 확인
docker logs <컨테이너ID>